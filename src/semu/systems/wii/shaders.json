{
  "schema_version": 1,
  "enabled": false,
  "doc": { "note": "Standalone Dolphin: presentation comes from the GL tap + bezels.json; vkBasalt fallback uses the vkbasalt pack via dolphin's overlay_aspect." }
}

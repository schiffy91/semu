{
  "schema_version": 1,
  "enabled": false,
  "doc": { "note": "Standalone PCSX2: presentation comes from the GL tap + bezels.json; vkBasalt fallback uses the vkbasalt pack via pcsx2's overlay_aspect." }
}

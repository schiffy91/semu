{
  "name": "payload-audit",
  "state": "payload-audit",
  "stateMaterial": "{{sourceHash}}",
  "operations": [
    {
      "op": "require-command",
      "command": "git"
    },
    {
      "op": "require-command",
      "command": "make"
    },
    {
      "op": "artifact",
      "path": ".gitignore"
    },
    {
      "op": "host",
      "command": "git check-ignore -q src/generated/test/vms && ! git check-ignore -q build && ! git check-ignore -q .semu && ! git check-ignore -q deck-shots && printf generated-only",
      "expect": "generated-only"
    },
    {
      "op": "payload-audit"
    }
  ]
}

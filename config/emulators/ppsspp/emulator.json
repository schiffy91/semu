{
  "schema_version": 1,
  "name": "PPSSPP",
  "default_system": "psp",
  "input": {
    "actions": [
      "ui.pause",
      "ui.screenshot",
      "ui.fullscreen",
      "ui.menu",
      "app.quit",
      "state.prev",
      "state.next",
      "state.load",
      "state.save",
      "speed.fast"
    ]
  },
  "platforms": {
    "linux": {
      "executable": "${asset_root}/bin/ppsspp",
      "args": [
        "--fullscreen"
      ],
      "graphics": {
        "method": "config_file",
        "file": "${state_root}/config/ppsspp/PSP/SYSTEM/ppsspp.ini",
        "format": "ini",
        "section": "Graphics",
        "key": "GraphicsBackend",
        "values": {
          "opengl": "OPENGL"
        },
        "extra_pins": [
          {
            "section": "Graphics",
            "key": "DisabledGraphicsBackends",
            "value": "VULKAN,DIRECT3D11,DIRECT3D9"
          },
          {
            "section": "Graphics",
            "key": "EnableCardboardVR",
            "value": "False"
          },
          {
            "section": "Graphics",
            "key": "InternalScreenRotation",
            "value": "1"
          }
        ]
      },
      "state": {
        "config_home": "xdg",
        "data_home": "xdg",
        "config_subdir": "ppsspp",
        "data_subdir": "ppsspp"
      },
      "render_preload": true
    }
  },
  "doc": {
    "launch": "The bundled source-built SDL frontend receives --fullscreen and exactly one positional ROM appended by the launcher. No host executable, Flatpak, libretro core, or prebuilt binary is permitted.",
    "state": "PPSSPP maps $XDG_CONFIG_HOME/ppsspp to isolated Semu state. Generated settings live in PSP/SYSTEM, game saves in PSP/SAVEDATA, save states in PSP/PPSSPP_STATE, and cache/screenshots remain below the same isolated root.",
    "input": "Pad zero is PPSSPP's SDL GameController lane for the Steam Virtual Gamepad. PSP controls are explicit, Semu keymap actions compile to PPSSPP chords, and the target process-group watcher owns immediate physical Start+Select termination.",
    "rendering": "The managed profile pins OpenGL. libsemupreload (render_preload) composes Semu's bezel and shader at the SDL/EGL swap: it reads the emulator's back buffer and writes the composed frame back before the real swap."
  }
}
